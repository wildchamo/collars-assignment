meta {
  name: login user
  type: http
  seq: 5
}

post {
  url: http://localhost:8787/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "SecurePass123!"
  }
}
