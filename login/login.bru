meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8787/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password"
  }
}
